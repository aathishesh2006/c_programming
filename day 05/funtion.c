#include <stdio.h>
int add (int x)
  {
    return x+x;
  }

  int main()
  {
    int arunkrishnan;
    arunkrishnan = add (10);
    printf("the sum of 10 addition is : %d", arunkrishnan);
    
    return 0;
  }